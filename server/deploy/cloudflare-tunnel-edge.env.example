# Set only the exact RFC1918 private IPv4 peer observed for the host-managed
# connector at the Docker edge. Do not use a CIDR, hostname, public address,
# or copied example.
CLOUDFLARED_TUNNEL_PEER_IP=
